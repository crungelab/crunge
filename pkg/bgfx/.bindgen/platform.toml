source = '../../depot/bgfx/include/bgfx/platform.h'
target = 'src/crunge/bgfx/bindings/platform.cpp'

module = '_bgfx'

# Flags that get passed to LLVM to parse the headers
flags = [
    '-x', 'c++',
    '-std=c++17',
    '-I../../lib/bgfx/include'
]

excludes = [
    # Problems
    # Wrapped
    # Internal / Ignored
    #Obsolete
]

overloaded = [
]
