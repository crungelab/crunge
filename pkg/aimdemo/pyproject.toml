[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "aimdemo"
version = "0.1.0"
description = 'Crunge ImGui Demo'
requires-python = ">=3.8"
authors = [{ name = "Kurtis Fields", email = "kurtisfields@gmail.com" }]

dependencies = [
    "arcade ~=2.6.17",
]
