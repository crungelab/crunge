[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "spine_demo"
version = "0.1.0"
description = 'Crunge Spine Demo'
requires-python = ">=3.10"
authors = [{ name = "Kurtis Fields", email = "kurtisfields@gmail.com" }]

dependencies = [
]

[project.scripts]
spine_demo = 'spine_demo.cli:cli'

[tool.hatch.envs.default]
python = "3.10"

